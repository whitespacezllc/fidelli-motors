// Los límites del programa. Son el CHECK de la columna premios.meta_services
// —la base es la que manda— repetidos acá para poder validar en el formulario
// y avisar antes de que el server rechace.
export const META_MINIMA = 2;
export const META_MAXIMA = 50;

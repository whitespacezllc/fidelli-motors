import type { Metadata } from "next";
import { PlaceholderSeccion } from "@/components/placeholder-seccion";

export const metadata: Metadata = { title: "Fidelización — Fidelli Motors" };

export default function Pagina() {
  return <PlaceholderSeccion titulo="Fidelización" descripcion="el premio activo, el ciclo de cada vehículo y los canjes" />;
}
